package package1;

public class Rectangle {
    public double area(double length, double breadth){
        return length * breadth;
    }
    public double peremeter(double length, double breadth){
        return 2*(length+breadth);
    }
}